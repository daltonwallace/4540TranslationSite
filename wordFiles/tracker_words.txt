@you might require incoming tickets are pre assigned to one of the available 
@when enabled some roles will be able to comment 
@when enabled some state changes may result in sending notifications to users when user is watching an issue. users can configure which event will notify 
@when enabled, each specific internal role in tracker (reporter, developer, resolvers, manager) will only have access to his accessible states against his 